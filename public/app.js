let token=localStorage.getItem('pm'),me,batches=[];const $=x=>document.getElementById(x);async function api(u,o={}){o.headers={...(o.headers||{}),Authorization:'Bearer '+token,'Content-Type':'application/json'};let r=await fetch(u,o),j=await r.json();if(!r.ok)throw Error(j.error);return j}async function login(){try{let r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:$('email').value,password:$('password').value})}),j=await r.json();if(!r.ok)throw Error(j.error);token=j.token;localStorage.setItem('pm',token);init()}catch(e){alert(e.message)}}function logout(){localStorage.removeItem('pm');location.reload()}async function init(){try{me=await api('/api/me');$('login').classList.add('hidden');$('app').classList.remove('hidden');$('role').textContent=me.role.toUpperCase();if(me.role!=='owner')$('usersBtn').classList.add('hidden');await load();show('dashboard')}catch(e){logout()}}function show(id){document.querySelectorAll('main section').forEach(s=>s.classList.add('hidden'));$(id).classList.remove('hidden');if(id==='users')users();if(id==='batches')listBatches()}async function load(){batches=await api('/api/batches');$('dashBatch').innerHTML=batches.map(b=>`<option value="${b.id}">${b.name}</option>`).join('');$('dBatch').innerHTML=batches.map(b=>`<option value="${b.id}">${b.name}</option>`).join('');if(batches[0])report(batches[0].id)}async function report(id){if(!id)return;let r=await api('/api/report/'+id);$('stats').innerHTML=[['Live Birds',r.live],['Feed',(+r.feed).toFixed(1)+' kg'],['Weight',(+r.weight).toFixed(1)+' kg'],['FCR',(+r.fcr).toFixed(2)],['Cost / Kg','₹'+(+r.costKg).toFixed(2)],['Total Cost','₹'+(+r.totalCost).toFixed(0)],['Sale Value','₹'+(+r.sale).toFixed(0)],['Profit','₹'+(+r.profit).toFixed(0)]].map(a=>`<div class="stat">${a[0]}<b>${a[1]}</b></div>`).join('')}async function daily(e){e.preventDefault();await api('/api/daily',{method:'POST',body:JSON.stringify({batch_id:+$('dBatch').value,date:$('date').value,feed_kg:+$('feed').value,dead:+$('dead').value,weight_kg:+$('weight').value,medicine:+$('med').value,other:+$('other').value,notes:$('notes').value})});alert('Daily entry saved');e.target.reset();$('date').value=new Date().toISOString().slice(0,10);load()}async function batch(e){e.preventDefault();await api('/api/batches',{method:'POST',body:JSON.stringify({name:$('bn').value,start_date:$('bd').value,chicks:+$('bc').value,chick_rate:+$('br').value,sale_rate:+$('sr').value})});alert('Batch created');e.target.reset();load();show('dashboard')}function listBatches(){$('bl').innerHTML=batches.map(b=>`<div class="row"><b>${b.name}</b><span>${b.chicks} chicks • ${b.start_date}</span></div>`).join('')}async function users(){if(me.role!=='owner')return;let u=await api('/api/users');$('ul').innerHTML=u.map(x=>`<div class="row"><span><b>${x.name}</b><br>${x.email}</span><span>${x.role} • ${x.active?'Active':'Off'}</span></div>`).join('')}async function user(e){e.preventDefault();try{await api('/api/users',{method:'POST',body:JSON.stringify({name:$('un').value,email:$('ue').value,password:$('up').value,role:$('ur').value})});alert('User added');e.target.reset();users()}catch(x){alert(x.message)}}if(token)init();
